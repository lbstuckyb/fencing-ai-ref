node_modules
dist
dist-ssr
coverage
*.local
.DS_Store

# Vendored MediaPipe assets (stage 4) — large binaries, reproduced by
# `npm run fetch-assets` (which also runs on postinstall)
public/models/*.task
public/mediapipe/

# Scenario clips (stage 15) — dropped in locally, not committed
public/scenarios/*.mp4
public/scenarios/*.webm

.vscode/*
!.vscode/extensions.json
.idea
*.log
npm-debug.log*

# Internal notes/docs — not for the public repo
docs/
