node_modules
dist
dist-ssr
coverage
*.local
.DS_Store

# Vendored MediaPipe model assets (stage 4) — large binaries, fetched by script
public/models/*.task

# Scenario clips (stage 15) — dropped in locally, not committed
public/scenarios/*.mp4
public/scenarios/*.webm

.vscode/*
!.vscode/extensions.json
.idea
*.log
npm-debug.log*
