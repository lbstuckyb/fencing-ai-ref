import ComingSoon from '../components/ComingSoon';
import PageHeader from '../components/PageHeader';

export default function PracticeSignals() {
  return (
    <>
      <PageHeader
        title="Signal practice"
        lede="Drill one signal at a time. The camera grades the shape of the gesture and the 1–2 second hold that t.63 requires, and tells you which part was wrong."
      />
      <ComingSoon stage="13">
        The drill needs the camera, the constraint evaluator and the hold machine first.
      </ComingSoon>
    </>
  );
}
