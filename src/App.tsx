/**
 * Stage 1 placeholder. The real app shell (routing, layout, nav) arrives in
 * stage 2 — see docs/PLAN.md.
 */
export default function App() {
  return (
    <main className="flex h-full flex-col items-center justify-center gap-2 p-8 text-center">
      <h1 className="text-2xl font-semibold">Fencing Referee Trainer</h1>
      <p className="text-sm opacity-70">Scaffold ready — app shell lands in stage 2.</p>
    </main>
  );
}
